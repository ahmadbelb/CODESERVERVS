namespace helper
{
	template<typename T, typename VectorType>
	inline bool areAllElementsGreaterThan(T value, const VectorType& vector)
	{
// implement a function that gives back one if all elements are greater than value and zero otherwise
	}

	template<typename VectorType1, typename VectorType2>
	inline double dotProduct(const VectorType1& v1, const VectorType2& v2)
	{
//implement a function that can compute the dot product of any type which provides the [] operator and the size() functions as an interface
	}
}
